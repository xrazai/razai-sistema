# raz-sistema

Desktop app — **Electron + Svelte + TypeScript + SQLite + CSS puro**.

UI: Industrial Brutalist Grid. Ver `AGENTS.md` e `docs/design-system.md`.

Caminho canônico do repositório: `C:\\Users\\razai\\Devs\\raz-sistema`

## Pré-requisitos (Windows)

- Git for Windows
- Node.js 22 LTS

## Comandos (PowerShell)

```powershell
cd C:\\Users\\razai\\Devs\\raz-sistema
npm install
npm run dev
npm run build
npm run typecheck
```
