# Migrations SQLite
#
# Coloque aqui arquivos versionados (ex.: 002_add_xyz.sql) quando o schema evoluir.
# Aplique-os de forma ordenada a partir de `db.ts` / um runner mínimo.
# Não crie um framework de migration até haver necessidade real.
